# V2 Cutover Evidence

Status: release candidate; owner approvals pending

Date: 2026-07-28

Source and artifact snapshot: commit `6da5ef6`

The implementation contracts under review are recorded in
`docs/V2_CUTOVER_POLICIES.md`; route ownership details remain in
`docs/route-ownership.md`.

## Automated gates

| Gate | Evidence |
| --- | --- |
| TypeScript | `npm run typecheck` |
| Unit and integration | `npm test` |
| Production artifact | `npm run build` |
| Single-file and credential-shape scan | `npm run test:artifact` |
| Production browser and `file://` | `npm run test:browser` |

Latest complete run on 2026-07-28: TypeScript passed; 109 Vitest files with
797 tests passed; the production build and artifact scan passed; 8 deterministic
production browser scenarios passed. Focused closure checks additionally cover
browser history, real template ingestion, v8-to-v9 IndexedDB preservation,
recovery state semantics, runtime hard limits, and partial multi-template
exports.

The production browser suite verifies:

- opening the HTML enters the V2 project home;
- project creation and opening remain in V2 chrome;
- historical project and Documents URLs resolve to V2;
- IndexedDB projects survive direct-route loading;
- the single-file artifact boots through `file://`;
- a persisted paused recipe resumes without replaying its completed stage;
- provider-call counts are one drafting call plus three audited editing calls;
- a critique-marked proposal remains interactive and non-mutating until accepted;
- the accepted edit survives reload; and
- the V2 top-bar export downloads the assembled freeform DOCX.
- browser Back/Forward restores Home, Library, and Settings views; and
- an uploaded DOCX is parsed, registered, and visible in Library immediately.

The default browser provider is an intercepted, deterministic OpenAI-compatible
transport. It exercises the production provider adapter and application code;
there is no test-only provider path in the release.

The opt-in real-provider lane was also run directly from
`release/index.html` over `file://`:

```text
V2_REAL_LOCAL_PROVIDER=1 npx playwright test tests/browser/v2-local-provider.spec.ts
```

It passed against Ollama 0.32.5 with `qwen3:0.6b`, using the production Local
OpenAI adapter at `http://127.0.0.1:11434/v1`, and produced a persisted
freeform draft in the V2 workspace.

## Desktop DOCX validation

Generated by:

```text
npx vite-node scripts/generate-docx-acceptance.ts D:\tmp\asksage-v2-docx-acceptance
```

Validated outputs:

| Application | Fixture | Result |
| --- | --- | --- |
| Microsoft Word 16.0 | `template-roundtrip.docx` | Opened through `Documents.OpenNoRepairDialog`; 52 paragraphs; no repair dialog |
| Microsoft Word 16.0 | `template-resumed-run.docx` | Opened through `Documents.OpenNoRepairDialog`; 56 paragraphs; no repair dialog |
| Microsoft Word 16.0 | `freeform-revised.docx` | Opened through `Documents.OpenNoRepairDialog`; 13 paragraphs; no repair dialog |
| LibreOffice 26.2.4.2 | `template-roundtrip.docx` | Opened and rendered to PDF successfully |
| LibreOffice 26.2.4.2 | `template-resumed-run.docx` | Opened and rendered to PDF successfully |
| LibreOffice 26.2.4.2 | `freeform-revised.docx` | Opened and rendered to PDF successfully |

The generated files and LibreOffice renderings are temporary validation
artifacts under `D:\tmp`; repository fixtures remain synthetic.

The final generator also writes `preservation-matrix.json`. ZIP and document
XML validation passed for all three outputs. The resumed-run fixture contains
and validates tables, rich runs, and a page break; template outputs retain
headers, footers, and styles. Focused assembler tests separately prove
numbering/list preservation and drawing-bearing header preservation.

## Remaining approvals

- Product, security, and release owners review and approve this evidence.
